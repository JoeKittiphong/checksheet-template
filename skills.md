# Skills & Development Guide

เอกสารนี้รวบรวมแนวทางการใช้งาน Component, มาตรฐานการตั้งชื่อ และ Logic การตรวจสอบข้อมูล (Validation) สำหรับโปรเจกต์ Checksheet Template

## 1. Naming Conventions (มาตรฐานการตั้งชื่อ)

เพื่อให้ข้อมูลทั้งหมดสามารถรวมกันเป็นก้อนเดียว (Global State) และรองรับระบบ Pagination Status ได้อย่างถูกต้อง ต้องปฏิบัติตามกฎดังนี้:

### Format: `p{PageNumber}_{Description}`

- **PageNumber**: เลขหน้าตามจริงของเอกสาร (Physical Page) เช่น `p1`, `p2`, `p37`
- **Description**: คำอธิบายสั้นๆ ของข้อมูลนั้น
- **ตัวอย่าง**:
  - `p1_machine_no`
  - `p15_insulation_check`
  - `p37_tension_val`

> **Note**: ห้ามใช้ชื่อซ้ำกันข้ามหน้า เพราะข้อมูลทั้งหมดจะถูก merge รวมกันในตอนท้าย

---

## 2. Component Usage & React Hook Form (RHF)

ทุก Input Component ในโปรเจกต์นี้ถูกออกแบบมาให้ทำงานร่วมกับ `react-hook-form` ผ่าน `useFormContext` โดยอัตโนมัติ

### 2.1 FormItemCheck (Checkbox + Input)
ใช้สำหรับหัวข้อตรวจสอบทั่วไปที่มี Checkbox และช่องกรอกค่า (Optional)

```jsx
<FormItemCheck 
    name="pXX_check_point" 
    label="1. ตรวจสอบความสะอาด" 
    input={{
        name: "pXX_check_val", // ชื่อตัวแปรสำหรับ Input ย่อย
        expectedValue: "OK",   // ค่าที่คาดหวัง (ตรวจสอบอัตโนมัติ)
        suffix: "จุด"
    }}
/>
```
- **Validation**: ทั้ง Checkbox และ Input จะถูกตั้งเป็น `required: true` โดยค่าเริ่มต้น

### 2.2 FormInputCheckSTD (Input พร้อมมาตรฐาน)
ใช้สำหรับช่องกรอกค่าที่มี Std Min/Max

```jsx
<FormInputCheckSTD 
    name="pXX_pressure" 
    label="Pressure Check" 
    minStd={50}
    maxStd={100}
    unit="Bar"
/>
```
- **Validation**:
  - **Required**: ต้องกรอกเสมอ (เว้นแตระบุ `required={false}`)
  - **Range**: ถ้ากรอกค่าหลุดจาก 50-100 พื้นหลังจะเปลี่ยนเป็นสีแดง

### 2.3 FormQuickTable (ตารางข้อมูล)
ใช้สร้างตารางที่มีความซับซ้อน รองรับ RowSpan และ Validation ในตัว

```jsx
<FormQuickTable
    columns={[
        { header: "Point", key: "point", width: "50px" },
        { header: "Value", key: "val", type: "input", width: "100px" }
    ]}
    data={[
        { point: "A1", val: "pXX_table_a1", pXX_table_a1_min: 10, pXX_table_a1_max: 20 },
        { point: "A2", val: "pXX_table_a2", pXX_table_a2_min: 10, pXX_table_a2_max: 20 },
    ]}
/>
```
- **Key Concept**: ระบุชื่อตัวแปรลงไปในช่อง `data` ได้เลย Component จะแปลงเป็น Input ให้เอง

---

## 3. Validation Logic (การตรวจสอบข้อมูล)

ระบบถูกตั้งค่าให้ **"บังคับกรอก 100%"** (Strict Validation) เพื่อป้องกัน Human Error

### 3.1 Default Behavior
- **Input/Checkbox**: ค่าว่าง (`""`, `null`, `undefined`) = **Error**
- **Table**: ทุกช่องในตารางต้องถูกกรอก
- **Signature**: ต้องลงชื่อก่อนจึงจะกด Verify ผ่าน

### 3.2 Pagination Status
ระบบจะตรวจสอบความครบถ้วนของแต่ละหน้าเมื่อกดปุ่ม **Verify**:
- **🟢 สีเขียว**: กรอกครบทุกช่อง และค่าอยู่ในเกณฑ์มาตรฐาน
- **🔴 สีแดง**: มีช่องว่าง หรือค่าหลุดเกณฑ์
- **การเปลี่ยนสี**: ใช้ Logic เช็ค Error Key ที่ขึ้นต้นด้วย `p{PageNumber}_`

### 3.3 การยกเว้น (Optional Fields)
หากต้องการให้ช่องไหนไม่บังคับกรอก ต้องระบุ Prop `required={false}` อย่างชัดเจน:

```jsx
<FormInputCheckSTD 
    name="pXX_remark" 
    label="Remark" 
    required={false} // อนุญาตให้ว่างได้
/>
```

---

## 4. Tips for Developers

1.  **Always use `Control`**: อย่าใช้ `<input>` ธรรมดา ให้ใช้ Component ที่หุ้ม `Controller` มาให้แล้วเสมอ
2.  **Inspect RHF**: ใช้ `console.log(methods.getValues())` ใน `ChecksheetMaster` เพื่อดูโครงสร้างข้อมูลจริง
3.  **Unique Names**: ตรวจสอบชื่อตัวแปรให้ดี อย่า copy-paste แล้วลืมแก้ชื่อ (โดยเฉพาะในตาราง)

---

## 5. Image Assets Handling (การจัดการรูปภาพ)

หากใน Checksheet มีรูปภาพประกอบ (เช่น รูปเครื่องมือวัด, จุดตรวจสอบ) ให้ปฏิบัติดังนี้:

1.  **Directory**: เก็บไฟล์ภาพไว้ที่ `src/assets/{FormName}/` (สร้างโฟลเดอร์ตามชื่อ Form Name)
2.  **Naming**: ตั้งชื่อไฟล์ให้สื่อความหมายและมีลำดับข้อ เช่น `6_Oscilloscope.png` (สำหรับข้อ 6)
3.  **Import**: นำเข้าไฟล์ใน Page Component ดังนี้:

```jsx
import MyImage from '@/assets/FAWIxxxx_Vx/my_image.png';

// Usage
<img src={MyImage} alt="Description" className="w-[50%]" />
```

> **Note**: พยายามใช้รูปภาพจากต้นฉบับเพื่อให้ User เข้าใจได้ง่ายที่สุด

---

## 6. Advanced Technical Patterns (เทคนิคขั้นสูง)

### 6.1 Date Input Styling (จัดตัวหนังสือใน Input Date)
การจัด `text-center` ใน `input[type="date"]` มักมีปัญหากับ WebKit Browsers (Chrome/Edge/Safari) ให้ใช้ CSS Selector พิเศษดังนี้:

```css
/* Tailwind Class Equivalent */
[&::-webkit-date-and-time-value]:text-center 
[&::-webkit-datetime-edit]:flex 
[&::-webkit-datetime-edit]:justify-center
```
**ตัวอย่างการใช้ใน JSX**:
```jsx
<input 
    type="date" 
    className="... text-center [&::-webkit-date-and-time-value]:text-center [&::-webkit-datetime-edit]:flex [&::-webkit-datetime-edit]:justify-center"
/>
```
*เหตุผล: Browser จะสร้าง Shadow DOM ซ้อนทับ Input Date ปกติ เราต้องเจาะจงไปที่ `datetime-edit` element เพื่อจัดตำแหน่งข้อความ*

### 6.2 Keypad Architecture (ระบบ Keypad)
หากพบ Error `useKeypad is not defined` หรือแอป Crash เมื่อเข้าหน้า Form:
- **Root Cause**: ลืมใส่ `<KeypadProvider>` ในจุดสูงสุดของ App
- **Solution**: ตรวจสอบ `main.jsx` (หรือ `App.jsx`) ว่ามีการหุ้ม Provider ไว้หรือไม่:

```jsx
// main.jsx
<AuthProvider>
  <KeypadProvider> {/* ต้องอยู่ภายใน Auth แต่ภายนอก App */}
    <App />
  </KeypadProvider>
</AuthProvider>
```

### 6.3 PDF Naming Customization (ตั้งชื่อไฟล์ PDF)
การตั้งชื่อไฟล์ PDF ที่ได้จากการสั่ง Print (Ctrl+P) ขึ้นอยู่กับ `document.title` ณ ขณะนั้น:

**Pattern ใน `ChecksheetMaster.jsx`**:
```javascript
const handlePrint = () => {
    // 1. เก็บชื่อเดิม
    const originalTitle = document.title;
    
    // 2. สร้างชื่อใหม่ (Form-Ver-Model-Title-Machine)
    const newTitle = `FAWI0002-V3-AL40G-CheckSheet-MC001`;
    document.title = newTitle;

    // 3. สั่ง Print
    window.print();

    // 4. คืนค่าชื่อเดิม (Delay นิดหน่อยเพื่อให้ Browser จับชื่อทัน)
    setTimeout(() => document.title = originalTitle, 1000);
};
```

### 6.4 Role-Based Permissions Pattern (การจำกัดสิทธิ์ตาม Role)
สำหรับการจำกัดสิทธิ์ใน Component ส่วนกลาง (เช่น Cover Page) ให้ใช้ Pattern นี้:

```jsx
const { user } = useAuth();
const isWorker = user?.role === 'worker';

<input 
    name="machineNo"
    // 1. Worker แก้ไม่ได้
    readOnly={isWorker} 
    // 2. Disable Keypad Interaction
    onClick={(e) => !isWorker && handleInputClick(...)}
    // 3. Visual Feedback
    className={isWorker ? "bg-gray-300 cursor-not-allowed" : "bg-white"}
/>
```

---

## 7. Status Workflow & Enforcement

ระบบบังคับ Workflow การทำงานดังนี้:
1.  **Prepare**: สถานะเริ่มต้น (Draft)
2.  **Work in Progress**: เมื่อเริ่มมีการ Save หรือกรอกข้อมูล
3.  **Finish**: *ต้องกรอกครบ 100% เท่านั้น*

**Code Pattern สำหรับปุ่ม Finish**:
```javascript
// ProfileBar.jsx หรือจุดที่เรียก Save
const handleSubmit = async () => {
    // 1. Trigger Validation ทั้งหมดก่อน
    const isValid = await methods.trigger(); 
    
    if (!isValid) {
        alert("กรุณากรอกข้อมูลให้ครบถ้วน (ตรวจสอบช่องสีแดง)");
        return; // ห้ามไปต่อ
    }
    
    // 2. ถ้าผ่าน ค่อยส่ง status 'finish'
    onSave('finish');
};
```

---

## 8. Counter-Intuitive Spec Symbols (สัญลักษณ์เกณฑ์ตรวจสอบแบบพิเศษ)

ใน Checksheet นี้ มีการตีความเครื่องหมายทางคณิตศาสตร์ในคอลัมน์ Spec ที่แตกต่างจากปกติ (Project Specific Rule):

### 8.1 Symbol `< X` (Less Than)
- **ความหมายปกติ**: น้อยกว่า X
- **ความหมายในโปรเจกต์นี้**: **ต้องมีค่าตั้งแต่ X ขึ้นไป** (Minimum Limit)
- **Validation Logic**: `min = X`, `max = 99999` (Infinite)
- **ตัวอย่าง**: Spec `< 4040` => ค่าที่ยอมรับได้คือ `4040` ถึง `99999`

### 8.2 Symbol `> X` (Greater Than)
- **ความหมายปกติ**: มากกว่า X
- **ความหมายในโปรเจกต์นี้**: **ห้ามเกิน X** (Maximum Limit)
- **Validation Logic**: `min = -9999` (Infinite), `max = X`
- **ตัวอย่าง**: Spec `> 9.0` => ค่าที่ยอมรับได้คือ `-9999` ถึง `9.0`

### 8.3 Symbol `Exact Value` (Fixed Target)
- **ลักษณะ**: ระบุค่าเลขโดดๆ (เช่น `040`, `0`, `500`)
- **ความหมาย**: **ต้องเท่ากับค่านั้นเป๊ะๆ** (Exact Match)
- **Validation Logic**: `min = X`, `max = X`
- **ตัวอย่าง**: Spec `040` => ค่าที่ยอมรับได้คือ `040` เท่านั้น

> **Note**: ตรรกะนี้ใช้เฉพาะกับช่องที่มีสัญลักษณ์กำกับใน Header หรือ Spec เท่านั้น หากเป็นช่องปกติให้ใช้ตรรกะตามจริง
